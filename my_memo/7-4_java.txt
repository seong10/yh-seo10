프로젝트 명 : PTest
src 폴더 안에
myPack 이름으로 패키지를 만든다.

패키지 Package
> 윈도우즈 : 폴더 , 리눅스 : 디렉토리 , 자바 : 패키지
	// 패키지의 이름은 소문자

패키지 안에 패키지를 또 생성
(기존패키지).(만들패키지)
ex) myPack.text

====
2. 접근제어자와 getter / setter 함수
3. 멤버변수와 로컬변수의 차이점
4. 클래스의 상속하는 코드 설명
5. 상속받을때, 부모클래스의 private 변수의 억세스는 제한된다.
6. 메소드 오버라이딩이란?
7. super 키워드

==== 메모리 순서
1. 메인부터 보고
= 의 오른쪽부터 new _Parent(); heap에 생성되고
main 함수안에 parent 생성되고

페어런트 다시 삭제
오른쪽 끝

메인함수에 p1이 생기고
p1 은 heap의 저장된걸 가르킨다
함수 끝나면 stack은 다 삭제
heap은 삭제 안됨 > 가비지컬렉터 jvm 이 선만있고 변수는 없네 하면 알아섯 삭제

==
heap에 name age money 들어갈칸 생기고

p1 이 heap을 가르킴

==
자바의 정석 책 복습하기